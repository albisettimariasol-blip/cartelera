import React, { useState, useEffect } from 'react';
import { initializeApp } from 'firebase/app';
import { getAuth, signInAnonymously, onAuthStateChanged } from 'firebase/auth';
import { getFirestore, doc, onSnapshot } from 'firebase/firestore';
import { 
  Instagram, 
  MapPin, 
  Globe, 
  MessageCircle, 
  ChevronRight,
  ExternalLink,
  BookOpen
} from 'lucide-react';

const firebaseConfig = JSON.parse(__firebase_config);
const app = initializeApp(firebaseConfig);
const auth = getAuth(app);
const db = getFirestore(app);
const appId = typeof __app_id !== 'undefined' ? __app_id : 'mes-19-redes';

export default function App() {
  const [user, setUser] = useState(null);
  const [data, setData] = useState({ 
    instagram: 'mes_isfd19',
    direccion: 'Almafuerte 565, Mar del Plata',
    web: 'isfd19-bue.infd.edu.ar'
  });

  useEffect(() => {
    const initAuth = async () => {
      if (typeof __initial_auth_token !== 'undefined' && __initial_auth_token) {
        await signInAnonymously(auth, __initial_auth_token);
      } else {
        await signInAnonymously(auth);
      }
    };
    initAuth();
    const unsubscribe = onAuthStateChanged(auth, setUser);
    return () => unsubscribe();
  }, []);

  const links = [
    {
      title: 'Instagram Estudiantil',
      subtitle: '@mes_isfd19',
      icon: <Instagram className="text-pink-500" />,
      url: `https://www.instagram.com/${data.instagram}/`,
      color: 'hover:bg-pink-50'
    },
    {
      title: 'Ubicación Sede',
      subtitle: data.direccion,
      icon: <MapPin className="text-red-500" />,
      url: `https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(data.direccion)}`,
      color: 'hover:bg-red-50'
    },
    {
      title: 'Sitio Web Oficial',
      subtitle: 'Información institucional',
      icon: <Globe className="text-blue-500" />,
      url: `https://${data.web}`,
      color: 'hover:bg-blue-50'
    },
    {
      title: 'Cartelera de Aulas',
      subtitle: 'Consulta horarios en tiempo real',
      icon: <BookOpen className="text-indigo-500" />,
      url: '#', // Aquí iría el link de la otra app
      color: 'hover:bg-indigo-50',
      highlight: true
    }
  ];

  return (
    <div className="min-h-screen bg-[#0f172a] flex items-center justify-center p-4 font-sans text-slate-200">
      <div className="w-full max-w-md space-y-8 animate-in fade-in zoom-in duration-500">
        
        {/* Profile Header */}
        <div className="text-center space-y-4">
          <div className="relative inline-block">
            <div className="w-24 h-24 bg-gradient-to-tr from-blue-600 to-indigo-600 rounded-3xl rotate-3 shadow-xl flex items-center justify-center border-2 border-white/10 overflow-hidden">
               <span className="text-4xl font-black italic -rotate-3 text-white">19</span>
            </div>
            <div className="absolute -bottom-1 -right-1 w-6 h-6 bg-green-500 border-4 border-[#0f172a] rounded-full"></div>
          </div>
          <div>
            <h1 className="text-2xl font-bold tracking-tight">MES ISFD 19</h1>
            <p className="text-slate-400 text-sm font-medium">Movimiento Estudiantil Organizado</p>
          </div>
        </div>

        {/* Links Container */}
        <div className="space-y-3">
          {links.map((link, idx) => (
            <a
              key={idx}
              href={link.url}
              target="_blank"
              rel="noopener noreferrer"
              className={`
                group flex items-center p-4 bg-slate-800/50 backdrop-blur-md border border-slate-700/50 
                rounded-2xl transition-all duration-300 hover:scale-[1.02] hover:bg-slate-800
                ${link.highlight ? 'ring-2 ring-indigo-500/50 bg-indigo-500/10' : ''}
              `}
            >
              <div className="w-12 h-12 bg-slate-900 rounded-xl flex items-center justify-center mr-4 group-hover:scale-110 transition-transform shadow-inner">
                {link.icon}
              </div>
              <div className="flex-1">
                <h3 className="font-bold text-sm text-white uppercase tracking-wide">{link.title}</h3>
                <p className="text-xs text-slate-400 font-medium">{link.subtitle}</p>
              </div>
              <ChevronRight className="text-slate-500 group-hover:text-white group-hover:translate-x-1 transition-all" size={20} />
            </a>
          ))}
        </div>

        {/* Footer */}
        <div className="text-center pb-8 pt-4">
          <p className="text-[10px] text-slate-500 font-bold uppercase tracking-[0.3em]">
            Mar del Plata • Argentina
          </p>
        </div>
      </div>
    </div>
  );
}
